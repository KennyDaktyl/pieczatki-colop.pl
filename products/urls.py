from django.urls import path
from django.conf import settings

urlpatterns = [
    # path('kategorie/', CategorysView.as_view(), name='categorys_view'),
    # path('kategorie/<slug:slug>',
    #      CategoryDetails.as_view(),
    #      name='category_details'),
]
